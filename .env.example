LLM_PROVIDER=ollama          # ollama | openai | mock
OLLAMA_MODEL=qwen2.5:7b
OLLAMA_HOST=http://localhost:11434
OPENAI_API_KEY=              # only used when LLM_PROVIDER=openai
OPENAI_MODEL=gpt-4o-mini     # example fallback model
ASSESS_TIMEOUT_SECONDS=30    # give up on the provider after this many seconds
